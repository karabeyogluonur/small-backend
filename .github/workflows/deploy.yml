name: Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install .NET 7
        run: |
          wget https://packages.microsoft.com/config/ubuntu/20.04/packages-microsoft-prod.deb -O packages-microsoft-prod.deb
          sudo dpkg -i packages-microsoft-prod.deb
          sudo apt-get update
          sudo apt-get install -y dotnet-sdk-7.0
          dotnet --version

      - name: Install .NET Core 7.0.11 framework
        run: |
          sudo apt-get install -y dotnet-runtime-7.0.11

      - name: Copy missing framework
        run: |
          mkdir -p ~/.dotnet/shared/Microsoft.NETCore.App/7.0.11/
          cp -r dotnet_frameworks/. ~/.dotnet/shared/Microsoft.NETCore.App/7.0.11/

      - name: Build .NET project
        run: |
          cd ${{ github.workspace }}
          dotnet publish --configuration Release --output ./small-backend .

      - name: Copy files using SCP
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh -o StrictHostKeyChecking=no -i ~/.ssh/id_rsa ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "iisreset /stop"
          scp -o StrictHostKeyChecking=no -i ~/.ssh/id_rsa -r ./small-backend/* ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/inetpub/sites/onurkarabeyoglu.xyz/
          ssh -o StrictHostKeyChecking=no -i ~/.ssh/id_rsa ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "iisreset /start"
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Status
        run: |
          echo "Deployment completed successfully."